name: CI
on:
  workflow_dispatch:

jobs:
  transform-test:
    runs-on: ubuntu-latest 
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
      - id: git-config
        shell: bash
        run: |
          git ls-remote https://github.com/ferdle-org/test-branch -q >/dev/null
